// Math

// abs(x)            Mengembalikan nilai absolut x
// acos(x)           Mengembalikan arccosine dari x, dalam radian
// acosh(x)          Mengembalikan arccosine hiperbolik dari x
// asin(x)           Mengembalikan arcsine dari x, dalam radian
// asinh(x)          Mengembalikan arcsine hiperbolik dari x
// atan(x)           Mengembalikan arctangent dari x sebagai nilai numerik antara -PI/2 dan PI/2 radian
// atan2(y, x)       Mengembalikan arctangent dari hasil bagi argumennya
// atanh(x)          Mengembalikan arctangent hiperbolik dari x
// cbrt(x)           Mengembalikan akar pangkat tiga dari x
// ceil(x)           Mengembalikan x, dibulatkan ke atas ke bilangan bulat terdekat
// cos(x)            Mengembalikan cosinus dari x (x dalam radian)
// cosh(x)           Mengembalikan cosinus hiperbolik dari x
// exp(x)            Mengembalikan nilai Ex
// floor(x)          Mengembalikan x, dibulatkan ke bawah ke bilangan bulat terdekat
// log(x)            Mengembalikan logaritma natural (basis E) dari x
// max(x, y, z, ...) Mengembalikan angka dengan nilai tertinggi
// min(x, y, z, ...) Mengembalikan angka dengan nilai terendah
// pow(x, y)         Mengembalikan nilai x ke pangkat y
// random()          Mengembalikan angka acak antara 0 dan 1
// round(x)          Membulatkan x ke bilangan bulat terdekat
// sign(x)           Mengembalikan jika x negatif, nol atau positif (-1, 0, 1)
// sin(x)            Mengembalikan sinus x (x dalam radian)
// sinh(x)           Mengembalikan sinus hiperbolik x
// sqrt(x)           Mengembalikan akar kuadrat x
// tan(x)            Mengembalikan tangen sudut
// tanh(x)           Mengembalikan tangen hiperbolik angka
// trunc(x)          Mengembalikan bagian integer dari angka (x)

let x = 893;
let y = 231;
let z = 383;

const hasilMax = Math.max(x, y, z)

console.log(x);