// Sets
// Salah satu objek bawaan JavaScript yang digunakan untuk menyimpan koleksi nilai yang bersifat unik (tidak ada duplikasi).

const angka = new Set([1, 2, 3, 4, 5]);

console.log(mySet); // Set(5) {1, 2, 3, 4, 5}