// Slice
// Method yang digunakan untuk menyalin sebagian elemen dari array atau string, tanpa mengubah array atau string asli.

let angka = [0, 1, 2, 3, 4, 5];

let hasil = angka.slice(1, 4); // slice(start, end);

console.log(hasil); // Output: [1, 2, 3]